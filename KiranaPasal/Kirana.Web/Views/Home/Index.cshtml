@model IEnumerable<Kirana.Core.Models.Products>

@{
    ViewBag.Title = "Home Page";
}
    <div class="jumbotron text-center">
        <h1 class="text-primary">Welcome to Kirana Pasal</h1>
        <small class="text-danger">Your Home to a new way of Shopping</small>
    </div>

    <hr />
    <h3 class="text-center">Our Products</h3>
    <hr />

   @foreach (var item in Model)
   {
<div class="col-md-4" style="height:450px;padding:1em;border:1px solid thin #D6DBDF">
    <div class="col-md-12">
        <img src="~/Content/ProductImages/@item.Image" alt="@item.Name" style="height:250px;" />
    </div>

    <div class="col-md-12">
        <strong> @Html.ActionLink(item.Name, "Details", new { id = item.Id })</strong>
    </div>
    <div class="col-md-12">
        <small>@item.description</small>
    </div>
    <div class="col-md-12">
        <p>@item.Price</p>
    </div>
    <div class="col-md-10 text-center">
        <a href="#" class="btn btn-success ">Add to Basket</a>
    </div>

</div>
   }
<div class="col-md-12 clearfix"  ></div>
